using System;

public class Kata
{
    public static long QueueTime(int[] customers, int n)
    {
        var tiles = new int[n];

        for (int i = 0; i < customers.Length; i++)
        {
            tiles[0] += customers[i];
            Array.Sort(tiles);
        }

        return tiles[n - 1];
    }
}
